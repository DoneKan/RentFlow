[build]
builder = "NIXPACKS"

[deploy]
startCommand = "npx prisma db push && node src/app.js"
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
