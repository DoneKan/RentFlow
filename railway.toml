[build]
builder = "NIXPACKS"

[deploy]
startCommand = "cd server && npx prisma db push && node src/app.js"
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
